/** @type {import('next').NextConfig} */
const nextConfig = {
    // Your existing configuration goes here
    // Note: The Sentry 'withSentryConfig' wrapper is no longer required in this file.
    // The Sentry SDK is now initialized in the 'instrumentation.ts' file.
};

export default nextConfig;
